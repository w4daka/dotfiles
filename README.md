# w4daka's dotfiles

- shell `zsh` + `sheldon` + `starship`
- editor `neovim`

## 個人的メモ

chezmoiを経由せずに変更したときは`chezmo add ~/hogehoge`する

### 画面の明るさを変更できようにする

```shell
sudo usermod -aG video $USER
```

をする
